import '../styles/Footer.css';
const Footer = () => {
    return (
        <footer>
            <p>CrowdFund — Together we make ideas happen.</p>
        </footer>
    );
};

export default Footer;
